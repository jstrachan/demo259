buildPack: maven
pipelineConfig:
  pipelines:
    pullRequest:
      build:
        preSteps:
        - sh: echo "before we build the PR"
    release:
      build:
        preSteps:
        - sh: echo "before the release builds"
